export type ActionType = "image" | "video" | "audio" | "confetti" | "wait" | "clear" | "stop";
export type QueueMode = "QUEUE" | "REPLACE" | "DROP";
export type EffectAction = { type: ActionType; durationMs: number; url?: string; volume?: number };
export type Effect = { id: string; projectId: string; name: string; actions: EffectAction[]; mode: QueueMode; cooldownMs: number; enabled: boolean };
export type DeckButton = { id: string; projectId: string; label: string; color: string; icon?: string; effectId: string; position: number; enabled: boolean; cooldownMs: number };
export type SessionUser = { id: string; username: string; role: "ADMIN" | "USER" };
export type AdminUser = { id: string; username: string; role: string; blocked: boolean; deckLimit: number; planExpiresAt: string | null; projectCount: number; createdAt: string };
export type Project = { id: string; name: string; description: string; userId: string; overlayToken: string };
export type Asset = { id: string; projectId: string; name: string; mime: string; url: string; size: number; sha256: string };
export type Settings = { projectId: string; volume: number; queueLimit: number; developerMode: boolean; userToken?: string };
